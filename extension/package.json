{
  "name": "toxiscope-extension",
  "version": "0.1.0",
  "description": "ToxiScope Browser Extension - Real-time gaming toxicity detection",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/content.ts src/popup.ts --bundle --format=esm --outdir=dist --target=es2020 && cp src/popup.html dist/popup.html",
    "watch": "esbuild src/content.ts src/popup.ts --bundle --format=esm --outdir=dist --target=es2020 --watch",
    "lint": "eslint src --ext .ts"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.266",
    "@types/node": "^22.7.4",
    "@eslint/js": "^9.9.0",
    "esbuild": "^0.23.0",
    "eslint": "^9.9.0",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.8.0"
  }
}
